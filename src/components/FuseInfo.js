import React from "react";

const FuseInfo = () => (
  <span className="FuseInfo">
    <span className="FuseInstructions">
      <span>
        <kbd>tab</kbd> or <kbd>↑↓</kbd> to navigate
      </span>
      <span>
        <kbd>enter</kbd> to select
      </span>
      <span>
        <kbd>tab</kbd> to dismiss
      </span>
    </span>
  </span>
);

export default FuseInfo;
